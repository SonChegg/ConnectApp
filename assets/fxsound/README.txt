Preferred bundled preset name:

default.fac

If default.fac is missing, the app will pick the first .fac file from this folder.

The app will expose a button to copy that preset into:

%APPDATA%\FxSound\Presets
